# CubeSat-Telemetry-Dashboard
Built a real-time CubeSat telemetry dashboard in Python using Streamlit to simulate satellite data loops. The backend applies atmospheric physics equations to dynamically scale temperature drop relative to altitude gain, featuring automated alert logic that flags system safety warnings if sub-zero or low-battery thresholds are met.
